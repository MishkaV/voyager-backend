# Supabase Configuration
# Required: Your Supabase project URL
SUPABASE_URL=https://your-project-id.supabase.co
# Required: Supabase service role API key (with full access)
# Get it from: Supabase Dashboard -> Settings -> API -> service_role key
SUPABASE_KEY=your-key-here
# Optional: Supabase storage bucket name for country flag images
# SUPABASE_BUCKET_NAME_FLAGS=flags

# RestCountries API Configuration
# Optional: Base URL for RestCountries API (default: https://restcountries.com/v3.1)
# RESTCOUNTRIES_BASE_URL=https://restcountries.com/v3.1

# Output Directory Configuration
# Optional: Directory name for seed SQL files (default: seeds)
# SEEDS_OUTPUT_DIR=seeds
